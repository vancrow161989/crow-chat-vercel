{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch run index.ts"
  },
  "_comment": "this is temporary. start - is built in comand so it command is 'bun start' without 'run' 'bun --watch run index.ts' but this dev is custom command so it needs to add 'run' - 'bun run dev'",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.6"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv": "^17.4.0",
    "express": "^5.2.1"
  }
}
